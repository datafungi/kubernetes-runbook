# OpenBao

Open-source secrets management (Vault-compatible fork). Stores and manages credentials consumed by the External Secrets Operator.

## Environments

| Directory | Environment     |
|-----------|-----------------|
| `k3d/`    | Local dev (k3d) |
