# External Secrets Operator

Kubernetes operator that syncs secrets from OpenBao (or other external stores) into native Kubernetes Secrets. Replaces manually-managed Secret manifests.

## Environments

| Directory | Environment     |
|-----------|-----------------|
| `k3d/`    | Local dev (k3d) |
